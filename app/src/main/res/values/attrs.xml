<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="ListFontStyle">
        <attr name="item_title_size" format="dimension" />
        <attr name="item_content_size" format="dimension" />
        <attr name="item_info_size" format="dimension" />
    </declare-styleable>

    <declare-styleable name="PostFontStyle">
        <attr name="post_title_size" format="dimension" />
        <attr name="post_content_size" format="dimension" />
        <attr name="post_info_size" format="dimension" />
    </declare-styleable>
</resources>
